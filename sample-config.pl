# Configuration file for wLex 3.1

# URI of your wLex installation (required if you run wLex via CGI)
$wlexURI = 'http://your.server/path/to/wlex/';

# Custom tracking code (e.g. for Google Analytics).
#$wlexTracker = qq#<!-- your tracking code -->#;

# Prefix for temporary files. You may also provide a full path if needed. Leave commented to use the default 'wlex-' in current directory.
#$wlexTMP = '/tmp/wlex-';

